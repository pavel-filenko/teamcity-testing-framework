name: Deploy backend

on: workflow_call

# Примерные шаги для развертывания бэкенда
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build backend
        run: echo Собирается билд бэкенда, выполняются юнит-тесты, формируется и публикуется Docker образ с изменениями

  deploy-stage:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy backend to stage/review app
        run: echo Происходит деплой бэкенда